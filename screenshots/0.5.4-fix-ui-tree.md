# 0.5.4-fix FarmScreen default UI tree

## Default path (`FarmScreen.kt`) — MUST NOT contain day-loop chrome
```
Box(fillMaxSize, SoftRanchSky + soft spring_ranch_base alpha)
  Column(fillMaxSize)                          // Scaffold already pads statusBars
    RanchTopBar(season / weather / streak / seeds / optional HKD)
    [optional clock-paused banner]
    RanchHotspotScene(weight 1, fillMaxWidth)  // full-bleed A2c base + 9 hotspots
    Row: TextButton 週回顧 | TextButton 設定   // discreet, not primary chrome
    [optional message]
```
Scaffold bottomBar = RanchBottomBar + navigationBars inset.

## Forbidden on default path (verified absent via FarmDefaultChromeGuardTest + rg)
- 起床橫幅 / 起床・新一日
- 今日帳 / 日結 CTA row / 商店 primary row
- 物品欄
- 選擇作物
- 等待・入晝／入昏／入夜 / 瞓覺過日
- FarmSceneLayer plots / LazyVerticalGrid 田格
- 餵寵物主 CTA row that dominated old shell

## Legacy (Settings →「牧場進階／舊農場」→ Routes.LEGACY_FARM)
`LegacyFarmDayLoopScreen.kt` hosts the old day-loop UI. Settlement rules remain in domain layer.

## Entry
`QuickEntryScreen`: wood clipboard near-fullscreen sheet (entry_clipboard), save → back to FARM.

## Insets
- Scaffold `contentWindowInsets = WindowInsets.statusBars`
- RanchBottomBar `Modifier.windowInsetsPadding(WindowInsets.navigationBars)`
- Settings / Legacy (no bottom bar): `navigationBars` padding on root
